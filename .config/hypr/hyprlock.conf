background {
    path = screenshot
    blur_passes = 3
    blur_size = 8
}

label {
    text = $TIME
    font_size = 70
    position = 0, 120
    halign = center
    valign = center
}

input-field {
    size = 300, 50
    position = 0, -80
    halign = center
    valign = center
}
