# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #

# you can choose themes from $HOME/.config/kitty/k
#shell zsh -i

include theme.conf
allow_remote_control yes
listen_on unix:@mykitty

font_family FantasqueSansM Nerd Font Mono Bold

font_size 13.0
bold_font auto
italic_font auto
bold_italic_font auto

background_opacity 0.80
dynamic_background_opacity 1
confirm_os_window_close 0

# Animated cursor
cursor_trail 1

# change to x11 or wayland or leave auto
linux_display_server auto

scrollback_lines 2000
wheel_scroll_min_lines 1

enable_audio_bell no

window_padding_width 4
# selection_foreground #111111
# selection_background #ff69b4
 #foreground #ff69b4
# background #ffd700
# cursor #ffff00


# BEGIN_KITTY_THEME
# Adwaita dark
include current-theme.conf
# END_KITTY_THEME

allow_remote_control yes
   listen_on unix:@mykitty


*(Note: If the terminal says the file doesn't exist, don't worry, Nano will create a clean new one for you.)*

2. Scroll to the very bottom of the file using your arrow keys and add this single line:
   ```text
   include ~/.cache/wal/colors-kitty.conf

2. Look for the `include ~/.cache/wal/colors-kitty.conf` line you added earlier, and **delete it** or change it to look exactly like this:
   ```text
   include theme.conf
